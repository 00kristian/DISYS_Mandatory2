syntax = "proto3";

package proto;

option go_package = "./proto";

service Token{
    rpc passToken(sendToken) returns (receiveToken);
}

message receiveToken{
    int64 id = 1;
}

message sendToken{
    int64 id = 1;
}
